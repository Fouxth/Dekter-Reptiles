import React, { useState, useEffect, useMemo } from 'react';
import { 
  ShoppingCart, Menu, X, ChevronRight, Search, 
  Filter, ShieldCheck, Truck, HeartHandshake, 
  Info, ArrowLeft, CheckCircle2, Trash2,
  BookOpen, MapPin, Phone, Mail, Clock, Send
} from 'lucide-react';

// ==========================================
// 1. Mock Data (ข้อมูลจำลองงูในร้าน)
// ==========================================
const MOCK_PRODUCTS = [
  {
    id: '1',
    name: 'Banana Cinnamon Ball Python',
    species: 'Ball Python',
    morph: 'Banana Cinnamon',
    scientificName: 'Python regius',
    price: 8500,
    gender: 'Male',
    age: '3 Months',
    diet: 'Frozen/Thawed Mice (Hopper)',
    description: 'งูหลามบอลมอร์ฟ Banana Cinnamon สีสันสวยงาม โทนเหลืองพาสเทล ตัดกับลวดลายสีน้ำตาลอ่อน กินเก่ง สุขภาพแข็งแรง ลอกคราบปกติ เหมาะสำหรับผู้เริ่มต้นและนักสะสม',
    careLevel: 'Beginner',
    image: 'https://images.unsplash.com/photo-1596443686812-2f45229eebc3?auto=format&fit=crop&q=80&w=800',
    inStock: true
  },
  {
    id: '2',
    name: 'Amel Corn Snake',
    species: 'Corn Snake',
    morph: 'Amelanistic',
    scientificName: 'Pantherophis guttatus',
    price: 3200,
    gender: 'Female',
    age: '2 Months',
    diet: 'Frozen/Thawed Pinky Mice',
    description: 'งูคอร์นสีส้มแดงสดใส ไม่มีเม็ดสีดำ (Amel) เลี้ยงง่ายมาก เชื่อง ไม่ดุ เหมาะมากสำหรับผู้ที่อยากเริ่มเลี้ยงงูตัวแรก ขนาดโตเต็มที่ไม่ใหญ่มาก',
    careLevel: 'Beginner',
    image: 'https://images.unsplash.com/photo-1531386151447-fd76ad50012f?auto=format&fit=crop&q=80&w=800',
    inStock: true
  },
  {
    id: '3',
    name: 'Western Hognose (Normal)',
    species: 'Hognose',
    morph: 'Normal',
    scientificName: 'Heterodon nasicus',
    price: 6500,
    gender: 'Male',
    age: '4 Months',
    diet: 'Frozen/Thawed Pinky Mice',
    description: 'งูจมูกหมูสุดน่ารัก หน้าตากวนๆ มีพฤติกรรมแกล้งตายเมื่อตกใจ กินอาหารเก่ง ไซส์กะทัดรัด พิษอ่อนมาก (ไม่เป็นอันตรายต่อมนุษย์ แต่ควรระวังสำหรับผู้แพ้)',
    careLevel: 'Intermediate',
    image: 'https://images.unsplash.com/photo-1615671524827-c1fe3973b648?auto=format&fit=crop&q=80&w=800',
    inStock: true
  },
  {
    id: '4',
    name: 'Blue Eyed Leucistic (BEL) Ball Python',
    species: 'Ball Python',
    morph: 'BEL (Super Mojave)',
    scientificName: 'Python regius',
    price: 18000,
    gender: 'Female',
    age: '5 Months',
    diet: 'Frozen/Thawed Mice (Adult)',
    description: 'งูหลามบอลสีขาวล้วน ตาสีฟ้า สวยงามดุจอัญมณี หายากและเป็นที่ต้องการสูง สุขภาพสมบูรณ์ กินอาหารสม่ำเสมอ',
    careLevel: 'Beginner',
    image: 'https://images.unsplash.com/photo-1518534089851-ce0b19688b1f?auto=format&fit=crop&q=80&w=800',
    inStock: true
  },
  {
    id: '5',
    name: 'Palmetto Corn Snake',
    species: 'Corn Snake',
    morph: 'Palmetto',
    scientificName: 'Pantherophis guttatus',
    price: 12000,
    gender: 'Male',
    age: '3 Months',
    diet: 'Frozen/Thawed Pinky Mice',
    description: 'มอร์ฟยอดฮิต ลำตัวสีขาว มีจุดสีแดงและเหลืองกระจายตามลำตัวเหมือนปลาคาร์ฟ สวยงามและโดดเด่นมาก เลี้ยงง่ายสไตล์คอร์นสเนค',
    careLevel: 'Beginner',
    image: 'https://images.unsplash.com/photo-1623843936932-a72eb3768482?auto=format&fit=crop&q=80&w=800',
    inStock: false
  }
];

const MOCK_ARTICLES = [
  {
    id: '1',
    title: 'คู่มือเลี้ยง Ball Python สำหรับมือใหม่ ฉบับสมบูรณ์',
    category: 'วิธีเลี้ยง',
    date: '15 พ.ค. 2026',
    excerpt: 'รวบรวมทุกสิ่งที่คุณต้องรู้ก่อนรับงูหลามบอลตัวแรกเข้าบ้าน ตั้งแต่การเตรียมอุปกรณ์ อุณหภูมิ ความชื้น ไปจนถึงการให้อาหาร',
    content: 'การเลี้ยง Ball Python หรือ งูหลามบอล เป็นที่นิยมมากสำหรับมือใหม่เพราะมีนิสัยเชื่อง ไม่ดุร้าย และมีสีสัน (Morph) ให้เลือกหลากหลาย\n\n1. การเตรียมตู้เลี้ยง (Enclosure)\nขนาดตู้ที่เหมาะสมสำหรับลูกงูคือ 10-20 แกลลอน และควรขยับขยายตามขนาดตัวเมื่อโตขึ้น\n\n2. อุณหภูมิและความชื้น\nอุณหภูมิฝั่งร้อน (Hot spot) ควรอยู่ที่ 31-33 องศาเซลเซียส และฝั่งเย็น 26-28 องศาเซลเซียส ความชื้นควรอยู่ที่ 55-60%\n\n3. การให้อาหาร\nควรให้หนูแช่แข็งที่นำมาละลายและอุ่นให้ได้อุณหภูมิ ขนาดของหนูไม่ควรใหญ่กว่าส่วนที่กว้างที่สุดของตัวงู ให้อาหารสัปดาห์ละ 1 ครั้งสำหรับงูเด็ก',
    image: 'https://images.unsplash.com/photo-1596443686812-2f45229eebc3?auto=format&fit=crop&q=80&w=800'
  },
  {
    id: '2',
    title: 'ทำไมงูถึงไม่ยอมกินอาหาร? สาเหตุและวิธีแก้ไข',
    category: 'ปัญหาพบบ่อย',
    date: '10 พ.ค. 2026',
    excerpt: 'ปัญหายอดฮิตของคนเลี้ยงงู เมื่อน้องงูปฏิเสธอาหาร มาดูสาเหตุหลักๆ เช่น เครียด กำลังจะลอกคราบ หรืออุณหภูมิไม่เหมาะสม',
    content: 'ปัญหางูไม่กินอาหาร (Going off feed) เป็นเรื่องที่พบได้บ่อย โดยเฉพาะในกลุ่ม Ball Python\n\nสาเหตุหลักๆ และวิธีแก้ไข ได้แก่:\n\n1. อุณหภูมิในตู้ไม่เหมาะสม: หากอากาศเย็นเกินไป ระบบย่อยอาหารของงูจะไม่ทำงานตามปกติ ทำให้พวกมันไม่อยากกิน ให้เช็คแผ่นความร้อนหรือไฟกก\n2. เข้าสู่ระยะลอกคราบ: สังเกตจากตาที่เริ่มขุ่น (ตาบลู) และสีตัวที่ซีดลง ช่วงนี้งูจะระแวงและไม่กินอาหาร ให้รอจนกว่าจะลอกคราบเสร็จ\n3. ความเครียด: อาจเกิดจากการถูกจับเล่นบ่อยเกินไป หรือไม่มีที่ซ่อนตัว (Hide box) ที่มิดชิดเพียงพอ\n4. อาการติดสัด: มักพบในงูตัวผู้ช่วงฤดูผสมพันธุ์ พวกมันจะโฟกัสที่การหาคู่มากกว่าการกิน ซึ่งถือเป็นเรื่องปกติ ให้หมั่นสังเกตน้ำหนักตัวแทน',
    image: 'https://images.unsplash.com/photo-1531386151447-fd76ad50012f?auto=format&fit=crop&q=80&w=800'
  },
  {
    id: '3',
    title: 'จัดตู้เลี้ยง Corn Snake อย่างไรให้สวยและอยู่สบาย',
    category: 'การจัดตู้เลี้ยง',
    date: '2 พ.ค. 2026',
    excerpt: 'ไอเดียการจัดตู้สำหรับงูคอร์นสเนค งูที่ชอบเลื้อยสำรวจ พร้อมแนะนำวัสดุรองพื้นและที่ซ่อน (Hide box) ที่เหมาะสม',
    content: 'Corn Snake หรือ งูข้าวโพด เป็นงูที่ค่อนข้างแอคทีฟ ชอบเลื้อยสำรวจ ดังนั้นการจัดตู้เลี้ยงจึงควรมีมิติและสภาพแวดล้อมที่กระตุ้นพฤติกรรมตามธรรมชาติ\n\n1. วัสดุรองพื้น (Substrate)\nแนะนำให้ใช้วัสดุจำพวกก้านปอ (Aspen Shavings) เพราะงูคอร์นชอบขุดคุ้ยและมุดซ่อนตัวใต้ดิน การใช้ก้านปอจะช่วยรักษาอุโมงค์ที่งูขุดไว้ได้ดี\n\n2. กิ่งไม้และของตกแต่ง\nควรใส่กิ่งไม้จำลองหรือกิ่งไม้จริง (ที่ผ่านการทำความสะอาดและอบฆ่าเชื้อแล้ว) เพื่อให้งูได้เลื้อยปีนป่าย ช่วยให้ออกกำลังกายและลดความเครียด\n\n3. ที่ซ่อนตัว (Hide Box)\nต้องมีอย่างน้อย 2 จุด คือฝั่งร้อนและฝั่งเย็น เพื่อให้งูสามารถเลือกซ่อนตัวและปรับอุณหภูมิร่างกายตัวเอง (Thermoregulation) ได้ตามต้องการ',
    image: 'https://images.unsplash.com/photo-1615671524827-c1fe3973b648?auto=format&fit=crop&q=80&w=800'
  }
];

// ==========================================
// 2. SEO Helper Component (ช่วยอัปเดต Meta Tags)
// ==========================================
const SEO = ({ title, description }) => {
  useEffect(() => {
    document.title = title ? `${title} | สยามเรปไทล์ (Siam Reptiles)` : 'สยามเรปไทล์ | ร้านขายงูออนไลน์ Ball Python, Corn Snake อันดับ 1';
    
    // Update or create meta description
    let metaDesc = document.querySelector('meta[name="description"]');
    if (!metaDesc) {
      metaDesc = document.createElement('meta');
      metaDesc.name = 'description';
      document.head.appendChild(metaDesc);
    }
    metaDesc.content = description || 'ร้านขายงูสายพันธุ์แท้ Ball Python, Corn Snake, Hognose พร้อมให้คำปรึกษาการเลี้ยงดูจากผู้เชี่ยวชาญ สุขภาพดีทุกตัว ส่งตรงถึงบ้าน';
  }, [title, description]);

  return null;
};

// ==========================================
// 3. Components
// ==========================================

// Format Currency THB
const formatPrice = (price) => {
  return new Intl.NumberFormat('th-TH', { style: 'currency', currency: 'THB', minimumFractionDigits: 0 }).format(price);
};

export default function App() {
  // Routing State
  const [currentPage, setCurrentPage] = useState('home'); // home, shop, product, cart, articles, contact, article, checkoutSuccess
  const [selectedProductId, setSelectedProductId] = useState(null);
  const [selectedArticleId, setSelectedArticleId] = useState(null);
  
  // UI State
  const [isMobileMenuOpen, setIsMobileMenuOpen] = useState(false);
  const [cart, setCart] = useState([]);
  const [searchQuery, setSearchQuery] = useState('');
  const [toast, setToast] = useState(null);
  
  // Notification Helper
  const showToast = (message) => {
    setToast(message);
    setTimeout(() => setToast(null), 3000);
  };

  // Navigation Helper
  const navigate = (page, productId = null, articleId = null) => {
    setCurrentPage(page);
    setSelectedProductId(productId);
    setSelectedArticleId(articleId);
    setIsMobileMenuOpen(false);
    window.scrollTo(0, 0);
  };

  // Search Handler
  const handleSearch = (e) => {
    const query = e.target.value;
    setSearchQuery(query);
    if (query && currentPage !== 'shop') {
      navigate('shop');
    }
  };

  // Cart Functions
  const addToCart = (product) => {
    setCart(prev => {
      const existing = prev.find(item => item.id === product.id);
      if (existing) {
        return prev.map(item => item.id === product.id ? { ...item, quantity: item.quantity + 1 } : item);
      }
      return [...prev, { ...product, quantity: 1 }];
    });
    showToast(`เพิ่ม ${product.name} ลงในตะกร้าแล้ว`);
  };

  const removeFromCart = (productId) => {
    setCart(prev => prev.filter(item => item.id !== productId));
  };

  const updateQuantity = (productId, delta) => {
    setCart(prev => prev.map(item => {
      if (item.id === productId) {
        const newQuantity = Math.max(1, item.quantity + delta);
        return { ...item, quantity: newQuantity };
      }
      return item;
    }));
  };

  const cartTotal = cart.reduce((sum, item) => sum + (item.price * item.quantity), 0);
  const cartItemCount = cart.reduce((sum, item) => sum + item.quantity, 0);

  // --- Sub-Pages Components ---

  const HomePage = () => (
    <main className="animate-fade-in">
      <SEO 
        title="หน้าแรก" 
        description="ยินดีต้อนรับสู่ สยามเรปไทล์ ศูนย์รวมงูเลี้ยงสวยงาม Ball Python, Corn Snake ที่ใหญ่ที่สุด พร้อมจัดส่งทั่วประเทศ" 
      />
      
      {/* Hero Section */}
      <section className="relative bg-stone-900 text-white overflow-hidden">
        <div className="absolute inset-0 z-0 opacity-40">
          <img 
            src="https://images.unsplash.com/photo-1596443686812-2f45229eebc3?auto=format&fit=crop&q=80&w=1920" 
            alt="งูหลามบอล" 
            className="w-full h-full object-cover"
          />
        </div>
        <div className="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-24 md:py-32">
          <h1 className="text-4xl md:text-6xl font-bold tracking-tight mb-4">
            ค้นพบเพื่อนร่วมโลก<br/><span className="text-emerald-400">สายพันธุ์พิเศษ</span> ของคุณ
          </h1>
          <p className="text-lg md:text-xl max-w-2xl text-stone-300 mb-8">
            สยามเรปไทล์ คัดสรรงูสายพันธุ์สวยงาม สุขภาพดี พร้อมให้คำปรึกษาการเลี้ยงดูอย่างถูกวิธีโดยผู้เชี่ยวชาญ (เชื่อมต่อสต๊อกจริงกับระบบ POS หน้าร้าน)
          </p>
          <button 
            onClick={() => navigate('shop')}
            className="bg-emerald-500 hover:bg-emerald-600 text-white font-semibold py-3 px-8 rounded-full transition-all flex items-center gap-2"
          >
            เลือกชมงูทั้งหมด <ChevronRight size={20} />
          </button>
        </div>
      </section>

      {/* Features */}
      <section className="bg-stone-50 py-12">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
            <div className="flex flex-col items-center text-center p-6 bg-white rounded-2xl shadow-sm border border-stone-100">
              <ShieldCheck className="w-12 h-12 text-emerald-500 mb-4" />
              <h3 className="text-xl font-semibold mb-2 text-stone-800">รับประกันสุขภาพ</h3>
              <p className="text-stone-600">งูทุกตัวผ่านการตรวจสุขภาพ ถ่ายพยาธิ และฝึกกินอาหารเรียบร้อยแล้ว</p>
            </div>
            <div className="flex flex-col items-center text-center p-6 bg-white rounded-2xl shadow-sm border border-stone-100">
              <Truck className="w-12 h-12 text-emerald-500 mb-4" />
              <h3 className="text-xl font-semibold mb-2 text-stone-800">จัดส่งปลอดภัย</h3>
              <p className="text-stone-600">แพ็คสินค้าอย่างระมัดระวัง จัดส่งด่วนพิเศษถึงหน้าบ้านคุณอย่างปลอดภัย</p>
            </div>
            <div className="flex flex-col items-center text-center p-6 bg-white rounded-2xl shadow-sm border border-stone-100">
              <HeartHandshake className="w-12 h-12 text-emerald-500 mb-4" />
              <h3 className="text-xl font-semibold mb-2 text-stone-800">ให้คำปรึกษาตลอดชีพ</h3>
              <p className="text-stone-600">ทีมงานพร้อมดูแลและให้คำแนะนำการเลี้ยงตั้งแต่เริ่มต้นจนเป็นมือโปร</p>
            </div>
          </div>
        </div>
      </section>

      {/* Featured Products */}
      <section className="py-16 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-end mb-8">
          <div>
            <h2 className="text-3xl font-bold text-stone-800 mb-2">งูมาใหม่แนะนำ</h2>
            <p className="text-stone-600">คัดพิเศษ ลวดลายสวย สุขภาพแข็งแรง</p>
          </div>
          <button onClick={() => navigate('shop')} className="hidden md:flex text-emerald-600 font-medium hover:text-emerald-700 items-center">
            ดูทั้งหมด <ChevronRight size={18} />
          </button>
        </div>
        
        <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
          {MOCK_PRODUCTS.slice(0, 4).map(product => (
            <ProductCard key={product.id} product={product} />
          ))}
        </div>
        <button 
          onClick={() => navigate('shop')} 
          className="md:hidden w-full mt-8 bg-stone-100 text-stone-800 font-medium py-3 rounded-xl flex justify-center items-center gap-2"
        >
          ดูทั้งหมด <ChevronRight size={18} />
        </button>
      </section>

      {/* Latest Articles Section */}
      <section className="py-16 bg-stone-50 border-t border-stone-100">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="flex justify-between items-end mb-8">
            <div>
              <h2 className="text-3xl font-bold text-stone-800 mb-2">เกร็ดความรู้ & วิธีเลี้ยง</h2>
              <p className="text-stone-600">คู่มือการดูแลฉบับสมบูรณ์จากผู้เชี่ยวชาญ</p>
            </div>
            <button onClick={() => navigate('articles')} className="hidden md:flex text-emerald-600 font-medium hover:text-emerald-700 items-center">
              ดูบทความทั้งหมด <ChevronRight size={18} />
            </button>
          </div>
          
          <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
            {MOCK_ARTICLES.slice(0, 3).map(article => (
              <article key={article.id} className="bg-white rounded-2xl shadow-sm border border-stone-100 overflow-hidden hover:shadow-md transition-shadow flex flex-col cursor-pointer" onClick={() => navigate('article', null, article.id)}>
                <div className="h-48 overflow-hidden relative">
                  <img src={article.image} alt={article.title} className="w-full h-full object-cover hover:scale-105 transition-transform duration-500" />
                  <span className="absolute top-4 left-4 bg-emerald-600 text-white text-xs font-bold px-3 py-1 rounded-full">
                    {article.category}
                  </span>
                </div>
                <div className="p-6 flex flex-col flex-1">
                  <span className="text-stone-400 text-sm mb-2">{article.date}</span>
                  <h3 className="text-xl font-bold text-stone-800 mb-3 line-clamp-2">{article.title}</h3>
                  <p className="text-stone-600 text-sm mb-4 line-clamp-3 flex-1">{article.excerpt}</p>
                  <button className="text-emerald-600 font-semibold hover:text-emerald-700 flex items-center gap-1 mt-auto">
                    อ่านเพิ่มเติม <ChevronRight size={16} />
                  </button>
                </div>
              </article>
            ))}
          </div>
          <button 
            onClick={() => navigate('articles')} 
            className="md:hidden w-full mt-8 bg-white border border-stone-200 text-stone-800 font-medium py-3 rounded-xl flex justify-center items-center gap-2"
          >
            ดูบทความทั้งหมด <ChevronRight size={18} />
          </button>
        </div>
      </section>

      {/* Contact CTA Section */}
      <section className="py-20 relative overflow-hidden">
        <div className="absolute inset-0 bg-stone-900 z-0"></div>
        <div className="absolute inset-0 z-0 opacity-20 bg-[url('https://images.unsplash.com/photo-1518534089851-ce0b19688b1f?auto=format&fit=crop&q=80&w=1920')] bg-cover bg-center"></div>
        <div className="relative z-10 max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center text-white">
          <h2 className="text-3xl md:text-5xl font-bold mb-6">มีข้อสงสัยเกี่ยวกับการเลี้ยงงู?</h2>
          <p className="text-lg text-stone-300 mb-10 max-w-2xl mx-auto">
            ไม่ว่าคุณจะเป็นมือใหม่หรือผู้เชี่ยวชาญ ทีมงานสยามเรปไทล์พร้อมให้คำปรึกษา แนะนำการเลือกงูที่เหมาะกับไลฟ์สไตล์ของคุณ และวิธีดูแลอย่างถูกต้อง
          </p>
          <div className="flex flex-col sm:flex-row gap-4 justify-center">
            <button 
              onClick={() => navigate('contact')}
              className="bg-emerald-500 hover:bg-emerald-600 text-white font-bold py-4 px-8 rounded-full transition-colors flex items-center justify-center gap-2"
            >
              <Mail size={20} /> ติดต่อสอบถามเรา
            </button>
            <a href="tel:0801234567" className="bg-white/10 hover:bg-white/20 backdrop-blur-sm text-white font-bold py-4 px-8 rounded-full transition-colors border border-white/20 flex items-center justify-center gap-2">
              <Phone size={20} /> 080-123-4567
            </a>
          </div>
        </div>
      </section>
    </main>
  );

  const ShopPage = () => {
    const [filter, setFilter] = useState('All');
    
    const filteredProducts = useMemo(() => {
      let result = MOCK_PRODUCTS;
      
      // Filter by Species
      if (filter !== 'All') {
        result = result.filter(p => p.species === filter);
      }
      
      // Filter by Search Query
      if (searchQuery) {
        const lowerQuery = searchQuery.toLowerCase();
        result = result.filter(p => 
          p.name.toLowerCase().includes(lowerQuery) || 
          p.morph.toLowerCase().includes(lowerQuery) ||
          p.species.toLowerCase().includes(lowerQuery)
        );
      }
      
      return result;
    }, [filter, searchQuery]);

    return (
      <main className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 animate-fade-in">
        <SEO 
          title="เลือกซื้องู" 
          description="เลือกซื้องู Ball Python, Corn Snake, Hognose หลากหลายมอร์ฟ อัปเดตสต๊อกล่าสุดพร้อมราคาที่ชัดเจน" 
        />
        
        <div className="flex flex-col md:flex-row md:items-center justify-between mb-8 gap-4">
          <div>
            <h1 className="text-3xl font-bold text-stone-800">เลือกชมสินค้า</h1>
            <p className="text-stone-600 mt-1">พบกับงูสวยงามกว่า {MOCK_PRODUCTS.length} รายการ (เชื่อมต่อสต๊อก POS)</p>
          </div>
          
          <div className="flex items-center gap-2 overflow-x-auto pb-2 md:pb-0 hide-scrollbar">
            <Filter size={20} className="text-stone-400 mr-2 flex-shrink-0" />
            {['All', 'Ball Python', 'Corn Snake', 'Hognose'].map(species => (
              <button
                key={species}
                onClick={() => setFilter(species)}
                className={`whitespace-nowrap px-4 py-2 rounded-full text-sm font-medium transition-colors ${
                  filter === species 
                  ? 'bg-emerald-600 text-white' 
                  : 'bg-stone-100 text-stone-600 hover:bg-stone-200'
                }`}
              >
                {species === 'All' ? 'ทั้งหมด' : species}
              </button>
            ))}
          </div>
        </div>

        {filteredProducts.length > 0 ? (
          <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
            {filteredProducts.map(product => (
              <ProductCard key={product.id} product={product} />
            ))}
          </div>
        ) : (
          <div className="text-center py-20">
            <p className="text-stone-500 text-lg">ไม่พบสินค้าในหมวดหมู่นี้</p>
          </div>
        )}
      </main>
    );
  };

  const ProductDetailPage = () => {
    const product = MOCK_PRODUCTS.find(p => p.id === selectedProductId);
    
    if (!product) return <div className="p-8 text-center">ไม่พบสินค้านี้</div>;

    return (
      <main className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 animate-fade-in">
        <SEO 
          title={`${product.name} - ${product.species}`} 
          description={`ขาย ${product.name} มอร์ฟ ${product.morph} ราคา ${product.price} บาท ร้านสยามเรปไทล์`} 
        />
        
        <button 
          onClick={() => navigate('shop')}
          className="flex items-center text-stone-500 hover:text-emerald-600 mb-6 transition-colors"
        >
          <ArrowLeft size={20} className="mr-1" /> กลับไปหน้าร้านค้า
        </button>

        <article className="bg-white rounded-3xl shadow-sm border border-stone-100 overflow-hidden">
          <div className="grid grid-cols-1 md:grid-cols-2 gap-0">
            {/* Image Section */}
            <div className="h-96 md:h-auto relative bg-stone-100">
              <img 
                src={product.image} 
                alt={`รูปภาพของงู ${product.name}`} 
                className="w-full h-full object-cover"
              />
              {!product.inStock && (
                <div className="absolute top-4 right-4 bg-red-500 text-white px-4 py-1 rounded-full text-sm font-bold shadow-lg">
                  ขายแล้ว (Sold Out)
                </div>
              )}
            </div>

            {/* Info Section */}
            <div className="p-8 md:p-12 flex flex-col justify-center">
              <div className="uppercase tracking-widest text-emerald-600 text-xs font-bold mb-2">
                {product.species}
              </div>
              <h1 className="text-3xl md:text-4xl font-bold text-stone-800 mb-2">
                {product.name}
              </h1>
              <p className="text-stone-400 italic mb-6 text-sm">{product.scientificName}</p>
              
              <div className="text-3xl font-bold text-emerald-600 mb-6">
                {formatPrice(product.price)}
              </div>

              <div className="prose prose-stone mb-8">
                <p>{product.description}</p>
              </div>

              <div className="grid grid-cols-2 gap-y-4 gap-x-6 mb-8 bg-stone-50 p-6 rounded-2xl">
                <div>
                  <span className="block text-xs text-stone-500 mb-1">มอร์ฟ (Morph)</span>
                  <span className="font-medium text-stone-800">{product.morph}</span>
                </div>
                <div>
                  <span className="block text-xs text-stone-500 mb-1">เพศ (Gender)</span>
                  <span className="font-medium text-stone-800">{product.gender}</span>
                </div>
                <div>
                  <span className="block text-xs text-stone-500 mb-1">อายุ (Age)</span>
                  <span className="font-medium text-stone-800">{product.age}</span>
                </div>
                <div>
                  <span className="block text-xs text-stone-500 mb-1">อาหาร (Diet)</span>
                  <span className="font-medium text-stone-800">{product.diet}</span>
                </div>
                <div>
                  <span className="block text-xs text-stone-500 mb-1">ระดับการเลี้ยง</span>
                  <span className="inline-flex items-center gap-1 font-medium text-stone-800">
                    <Info size={14} className="text-emerald-500"/> {product.careLevel}
                  </span>
                </div>
              </div>

              <button 
                onClick={() => addToCart(product)}
                disabled={!product.inStock}
                className={`w-full py-4 rounded-xl font-bold text-lg flex justify-center items-center gap-2 transition-all
                  ${product.inStock 
                    ? 'bg-stone-800 hover:bg-stone-900 text-white shadow-lg hover:shadow-xl' 
                    : 'bg-stone-200 text-stone-400 cursor-not-allowed'}`}
              >
                <ShoppingCart size={24} />
                {product.inStock ? 'เพิ่มลงตะกร้า' : 'สินค้าหมดชั่วคราว'}
              </button>
            </div>
          </div>
        </article>
      </main>
    );
  };

  const CartPage = () => (
    <main className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8 animate-fade-in min-h-[70vh]">
      <SEO title="ตะกร้าสินค้า" description="ตรวจสอบรายการสั่งซื้องูและสัตว์เลื้อยคลานของคุณ" />
      
      <h1 className="text-3xl font-bold text-stone-800 mb-8 flex items-center gap-3">
        <ShoppingCart className="text-emerald-600" size={32} /> ตะกร้าสินค้า
      </h1>

      {cart.length === 0 ? (
        <div className="bg-white rounded-3xl p-12 text-center border border-stone-100 shadow-sm">
          <div className="w-24 h-24 bg-stone-100 rounded-full flex items-center justify-center mx-auto mb-6">
            <ShoppingCart className="text-stone-300" size={48} />
          </div>
          <h2 className="text-xl font-semibold text-stone-800 mb-2">ตะกร้าของคุณยังว่างเปล่า</h2>
          <p className="text-stone-500 mb-8">ลองไปดูงูสวยๆ ในหน้าร้านค้าของเราสิครับ</p>
          <button 
            onClick={() => navigate('shop')}
            className="bg-emerald-600 hover:bg-emerald-700 text-white font-medium py-3 px-8 rounded-full transition-colors"
          >
            ไปหน้าร้านค้า
          </button>
        </div>
      ) : (
        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          <div className="lg:col-span-2 space-y-4">
            {cart.map(item => (
              <div key={item.id} className="bg-white p-4 rounded-2xl border border-stone-100 shadow-sm flex flex-col sm:flex-row gap-4 items-center">
                <img src={item.image} alt={item.name} className="w-24 h-24 object-cover rounded-xl" />
                <div className="flex-1 text-center sm:text-left">
                  <h3 className="font-bold text-stone-800 text-lg">{item.name}</h3>
                  <p className="text-stone-500 text-sm">{item.species}</p>
                  <div className="text-emerald-600 font-semibold mt-1">{formatPrice(item.price)}</div>
                </div>
                <div className="flex items-center gap-4">
                  <div className="flex items-center bg-stone-100 rounded-lg p-1">
                    <button onClick={() => updateQuantity(item.id, -1)} className="w-8 h-8 flex items-center justify-center rounded bg-white shadow-sm hover:bg-stone-50">-</button>
                    <span className="w-10 text-center font-medium">{item.quantity}</span>
                    <button onClick={() => updateQuantity(item.id, 1)} className="w-8 h-8 flex items-center justify-center rounded bg-white shadow-sm hover:bg-stone-50">+</button>
                  </div>
                  <button onClick={() => removeFromCart(item.id)} className="text-red-400 hover:text-red-600 p-2">
                    <Trash2 size={20} />
                  </button>
                </div>
              </div>
            ))}
          </div>
          
          <div className="lg:col-span-1">
            <div className="bg-white p-6 rounded-2xl border border-stone-100 shadow-sm sticky top-24">
              <h3 className="font-bold text-xl text-stone-800 mb-4">สรุปคำสั่งซื้อ</h3>
              <div className="space-y-3 mb-6 pb-6 border-b border-stone-100">
                <div className="flex justify-between text-stone-600">
                  <span>ยอดรวมสินค้า ({cartItemCount} ชิ้น)</span>
                  <span>{formatPrice(cartTotal)}</span>
                </div>
                <div className="flex justify-between text-stone-600">
                  <span>ค่าจัดส่ง</span>
                  <span className="text-emerald-600">ส่งฟรี</span>
                </div>
              </div>
              <div className="flex justify-between font-bold text-2xl text-stone-800 mb-8">
                <span>ยอดสุทธิ</span>
                <span className="text-emerald-600">{formatPrice(cartTotal)}</span>
              </div>
              <button 
                onClick={() => {
                  setCart([]);
                  navigate('checkoutSuccess');
                }}
                className="w-full bg-stone-900 hover:bg-stone-800 text-white font-bold py-4 rounded-xl flex justify-center items-center gap-2 transition-colors"
              >
                <CheckCircle2 size={20} /> ดำเนินการชำระเงิน
              </button>
            </div>
          </div>
        </div>
      )}
    </main>
  );

  const CheckoutSuccessPage = () => (
    <main className="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 py-20 animate-fade-in text-center min-h-[60vh] flex flex-col justify-center items-center">
      <SEO title="สั่งซื้อสำเร็จ" description="ขอบคุณที่สั่งซื้อสินค้ากับสยามเรปไทล์" />
      <div className="w-24 h-24 bg-emerald-100 text-emerald-600 rounded-full flex items-center justify-center mb-6">
        <CheckCircle2 size={48} />
      </div>
      <h1 className="text-4xl font-bold text-stone-800 mb-4">สั่งซื้อสำเร็จ!</h1>
      <p className="text-lg text-stone-600 mb-8">
        ขอบคุณที่ไว้วางใจสยามเรปไทล์ หมายเลขคำสั่งซื้อของคุณคือ <span className="font-bold text-emerald-600">#SR{Math.floor(Math.random() * 1000000).toString().padStart(6, '0')}</span><br/>
        <span className="text-sm mt-2 block text-stone-400">(นี่คือหน้าจอจำลองการสั่งซื้อ ในระบบจริงจะมีการเชื่อมต่อไปยัง POS เพื่อตัดสต๊อกทันที)</span>
      </p>
      <button 
        onClick={() => navigate('home')}
        className="bg-stone-900 hover:bg-stone-800 text-white font-bold py-4 px-8 rounded-xl transition-colors"
      >
        กลับสู่หน้าหลัก
      </button>
    </main>
  );

  const ArticleDetailPage = () => {
    const article = MOCK_ARTICLES.find(a => a.id === selectedArticleId);
    
    if (!article) return <div className="p-8 text-center">ไม่พบบทความนี้</div>;

    return (
      <main className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-12 animate-fade-in">
        <SEO title={article.title} description={article.excerpt} />
        
        <button 
          onClick={() => navigate('articles')}
          className="flex items-center text-stone-500 hover:text-emerald-600 mb-8 transition-colors"
        >
          <ArrowLeft size={20} className="mr-1" /> กลับไปหน้าบทความ
        </button>

        <article className="bg-white rounded-3xl shadow-sm border border-stone-100 overflow-hidden">
          <div className="h-64 md:h-96 w-full relative">
            <img src={article.image} alt={article.title} className="w-full h-full object-cover" />
            <div className="absolute top-6 left-6 bg-emerald-600 text-white text-sm font-bold px-4 py-1.5 rounded-full shadow-lg">
              {article.category}
            </div>
          </div>
          <div className="p-8 md:p-12">
            <div className="text-stone-400 text-sm mb-4">{article.date}</div>
            <h1 className="text-3xl md:text-4xl font-bold text-stone-800 mb-8 leading-tight">{article.title}</h1>
            <div className="prose prose-stone prose-lg max-w-none text-stone-600 whitespace-pre-line leading-relaxed">
              {article.content}
            </div>
          </div>
        </article>
      </main>
    );
  };

  const ArticlesPage = () => (
    <main className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12 animate-fade-in">
      <SEO title="บทความและวิธีเลี้ยง" description="แหล่งรวมความรู้ วิธีการเลี้ยงงู Ball Python, Corn Snake และการดูแลสัตว์เลื้อยคลานเบื้องต้น" />
      
      <div className="text-center mb-12">
        <h1 className="text-3xl md:text-4xl font-bold text-stone-800 mb-4 flex items-center justify-center gap-3">
          <BookOpen className="text-emerald-600" size={36} /> บทความและวิธีเลี้ยง
        </h1>
        <p className="text-stone-600 max-w-2xl mx-auto">รวบรวมความรู้ เคล็ดลับ และวิธีการดูแลเพื่อนร่วมโลกตัวน้อยของคุณจากประสบการณ์ของผู้เชี่ยวชาญ</p>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {MOCK_ARTICLES.map(article => (
          <article key={article.id} className="bg-white rounded-2xl shadow-sm border border-stone-100 overflow-hidden hover:shadow-md transition-shadow flex flex-col cursor-pointer" onClick={() => navigate('article', null, article.id)}>
            <div className="h-48 overflow-hidden relative">
              <img src={article.image} alt={article.title} className="w-full h-full object-cover hover:scale-105 transition-transform duration-500" />
              <span className="absolute top-4 left-4 bg-emerald-600 text-white text-xs font-bold px-3 py-1 rounded-full">
                {article.category}
              </span>
            </div>
            <div className="p-6 flex flex-col flex-1">
              <span className="text-stone-400 text-sm mb-2">{article.date}</span>
              <h3 className="text-xl font-bold text-stone-800 mb-3 line-clamp-2">{article.title}</h3>
              <p className="text-stone-600 text-sm mb-4 line-clamp-3 flex-1">{article.excerpt}</p>
              <button className="text-emerald-600 font-semibold hover:text-emerald-700 flex items-center gap-1 mt-auto">
                อ่านเพิ่มเติม <ChevronRight size={16} />
              </button>
            </div>
          </article>
        ))}
      </div>
    </main>
  );

  const ContactPage = () => {
    const [isSubmitted, setIsSubmitted] = useState(false);

    const handleSubmit = (e) => {
      e.preventDefault();
      setIsSubmitted(true);
    };

    return (
      <main className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12 animate-fade-in">
        <SEO title="ติดต่อเรา" description="ติดต่อสยามเรปไทล์ สอบถามข้อมูลสัตว์เลี้ยง ปรึกษาการดูแล หรือแจ้งปัญหาการใช้งาน" />
        
        <div className="max-w-4xl mx-auto">
          <div className="text-center mb-12">
            <h1 className="text-3xl md:text-4xl font-bold text-stone-800 mb-4">ติดต่อสยามเรปไทล์</h1>
            <p className="text-stone-600">เรายินดีให้คำปรึกษาและตอบทุกข้อสงสัยของคุณ</p>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-2 gap-12 bg-white rounded-3xl p-8 border border-stone-100 shadow-sm">
            {/* Contact Info */}
            <div>
              <h2 className="text-2xl font-bold text-stone-800 mb-6">ข้อมูลการติดต่อ</h2>
              <div className="space-y-6">
                <div className="flex items-start gap-4">
                  <div className="w-10 h-10 bg-emerald-50 text-emerald-600 rounded-full flex items-center justify-center flex-shrink-0">
                    <MapPin size={20} />
                  </div>
                  <div>
                    <h3 className="font-semibold text-stone-800">หน้าร้าน</h3>
                    <p className="text-stone-600 text-sm mt-1">123 ถนนสุขุมวิท แขวงคลองเตย เขตคลองเตย กรุงเทพมหานคร 10110</p>
                  </div>
                </div>
                
                <div className="flex items-start gap-4">
                  <div className="w-10 h-10 bg-emerald-50 text-emerald-600 rounded-full flex items-center justify-center flex-shrink-0">
                    <Phone size={20} />
                  </div>
                  <div>
                    <h3 className="font-semibold text-stone-800">เบอร์โทรศัพท์</h3>
                    <p className="text-stone-600 text-sm mt-1">080-123-4567</p>
                  </div>
                </div>

                <div className="flex items-start gap-4">
                  <div className="w-10 h-10 bg-emerald-50 text-emerald-600 rounded-full flex items-center justify-center flex-shrink-0">
                    <Mail size={20} />
                  </div>
                  <div>
                    <h3 className="font-semibold text-stone-800">อีเมล & Social</h3>
                    <p className="text-stone-600 text-sm mt-1">hello@siamreptiles.com</p>
                    <p className="text-stone-600 text-sm">Line: @siamreptiles</p>
                  </div>
                </div>

                <div className="flex items-start gap-4">
                  <div className="w-10 h-10 bg-emerald-50 text-emerald-600 rounded-full flex items-center justify-center flex-shrink-0">
                    <Clock size={20} />
                  </div>
                  <div>
                    <h3 className="font-semibold text-stone-800">เวลาทำการ</h3>
                    <p className="text-stone-600 text-sm mt-1">เปิดให้บริการทุกวัน 10:00 - 20:00 น.</p>
                  </div>
                </div>
              </div>
            </div>

            {/* Contact Form */}
            <div className="bg-stone-50 p-6 rounded-2xl flex flex-col justify-center min-h-[400px]">
              {isSubmitted ? (
                <div className="text-center py-8 animate-fade-in">
                  <div className="w-16 h-16 bg-emerald-100 text-emerald-600 rounded-full flex items-center justify-center mx-auto mb-4">
                    <CheckCircle2 size={32} />
                  </div>
                  <h3 className="text-2xl font-bold text-stone-800 mb-2">ส่งข้อความสำเร็จ</h3>
                  <p className="text-stone-600 mb-8">ทีมงานสยามเรปไทล์ได้รับข้อความของคุณแล้ว และจะติดต่อกลับโดยเร็วที่สุดครับ</p>
                  <button 
                    onClick={() => setIsSubmitted(false)}
                    className="text-emerald-600 font-semibold hover:text-emerald-700 underline"
                  >
                    ส่งข้อความใหม่
                  </button>
                </div>
              ) : (
                <>
                  <h2 className="text-xl font-bold text-stone-800 mb-4">ส่งข้อความหาเรา</h2>
                  <form className="space-y-4" onSubmit={handleSubmit}>
                    <div>
                      <label className="block text-sm font-medium text-stone-700 mb-1">ชื่อ-นามสกุล</label>
                      <input type="text" required className="w-full px-4 py-2 rounded-xl border-stone-200 shadow-sm focus:border-emerald-500 focus:ring focus:ring-emerald-200 transition-colors" placeholder="ชื่อของคุณ" />
                    </div>
                    <div>
                      <label className="block text-sm font-medium text-stone-700 mb-1">อีเมล หรือ เบอร์โทรศัพท์</label>
                      <input type="text" required className="w-full px-4 py-2 rounded-xl border-stone-200 shadow-sm focus:border-emerald-500 focus:ring focus:ring-emerald-200 transition-colors" placeholder="สำหรับติดต่อกลับ" />
                    </div>
                    <div>
                      <label className="block text-sm font-medium text-stone-700 mb-1">ข้อความ</label>
                      <textarea required rows="4" className="w-full px-4 py-2 rounded-xl border-stone-200 shadow-sm focus:border-emerald-500 focus:ring focus:ring-emerald-200 transition-colors" placeholder="พิมพ์ข้อความของคุณที่นี่..."></textarea>
                    </div>
                    <button type="submit" className="w-full bg-emerald-600 hover:bg-emerald-700 text-white font-bold py-3 px-6 rounded-xl transition-colors flex justify-center items-center gap-2">
                      <Send size={18} /> ส่งข้อความ
                    </button>
                  </form>
                </>
              )}
            </div>
          </div>
        </div>
      </main>
    );
  };

  // --- Shared Components ---
  const ProductCard = ({ product }) => (
    <article className="bg-white rounded-2xl shadow-sm border border-stone-100 overflow-hidden hover:shadow-md transition-shadow group flex flex-col h-full cursor-pointer" onClick={() => navigate('product', product.id)}>
      <div className="relative h-48 overflow-hidden bg-stone-100">
        <img 
          src={product.image} 
          alt={product.name} 
          className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
          loading="lazy"
        />
        {!product.inStock && (
          <div className="absolute inset-0 bg-white/60 backdrop-blur-sm flex items-center justify-center z-10">
            <span className="bg-stone-800 text-white px-4 py-2 rounded-full font-bold shadow-lg">Sold Out</span>
          </div>
        )}
        <div className="absolute top-2 left-2 bg-white/90 backdrop-blur-sm px-2 py-1 rounded text-xs font-semibold text-stone-700">
          {product.species}
        </div>
      </div>
      <div className="p-5 flex flex-col flex-1">
        <h3 className="text-lg font-bold text-stone-800 mb-1 line-clamp-1">{product.name}</h3>
        <p className="text-sm text-stone-500 mb-4 line-clamp-2 flex-1">{product.description}</p>
        <div className="flex items-center justify-between mt-auto">
          <span className="text-xl font-bold text-emerald-600">{formatPrice(product.price)}</span>
          <button 
            onClick={(e) => {
              e.stopPropagation();
              if(product.inStock) addToCart(product);
            }}
            disabled={!product.inStock}
            className={`w-10 h-10 rounded-full flex items-center justify-center transition-colors ${
              product.inStock ? 'bg-stone-100 hover:bg-emerald-600 hover:text-white text-stone-600' : 'bg-stone-100 text-stone-300 cursor-not-allowed'
            }`}
            aria-label="เพิ่มลงตะกร้า"
          >
            <ShoppingCart size={18} />
          </button>
        </div>
      </div>
    </article>
  );

  return (
    <div className="min-h-screen bg-stone-50 font-sans text-stone-900 flex flex-col">
      {/* Navigation */}
      <nav className="bg-white border-b border-stone-200 sticky top-0 z-50">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="flex justify-between h-20">
            {/* Logo */}
            <div className="flex items-center cursor-pointer" onClick={() => navigate('home')}>
              <div className="w-10 h-10 bg-emerald-600 rounded-lg flex items-center justify-center mr-3">
                <svg className="w-6 h-6 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M13 10V3L4 14h7v7l9-11h-7z" />
                </svg>
              </div>
              <div>
                <span className="font-extrabold text-xl text-stone-800 block leading-tight">สยามเรปไทล์</span>
                <span className="text-xs text-stone-500 font-medium tracking-wider">SIAM REPTILES</span>
              </div>
            </div>

            {/* Desktop Menu */}
            <div className="hidden md:flex items-center space-x-8">
              <button onClick={() => navigate('home')} className={`font-medium ${currentPage === 'home' ? 'text-emerald-600' : 'text-stone-600 hover:text-stone-900'}`}>หน้าแรก</button>
              <button onClick={() => navigate('shop')} className={`font-medium ${currentPage === 'shop' ? 'text-emerald-600' : 'text-stone-600 hover:text-stone-900'}`}>ร้านค้า</button>
              <button onClick={() => navigate('articles')} className={`font-medium ${currentPage === 'articles' ? 'text-emerald-600' : 'text-stone-600 hover:text-stone-900'}`}>บทความ/วิธีเลี้ยง</button>
              <button onClick={() => navigate('contact')} className={`font-medium ${currentPage === 'contact' ? 'text-emerald-600' : 'text-stone-600 hover:text-stone-900'}`}>ติดต่อเรา</button>
            </div>

            {/* Actions */}
            <div className="flex items-center space-x-4">
              <div className="hidden sm:block relative">
                <Search className="absolute left-3 top-1/2 -translate-y-1/2 text-stone-400" size={18} />
                <input 
                  type="text" 
                  placeholder="ค้นหางูมอร์ฟต่างๆ..." 
                  value={searchQuery}
                  onChange={handleSearch}
                  className="pl-10 pr-4 py-2 bg-stone-100 border-transparent rounded-full text-sm focus:border-emerald-500 focus:bg-white focus:ring-0 transition-all w-48 focus:w-64"
                />
              </div>
              <button 
                onClick={() => navigate('cart')}
                className="relative p-2 text-stone-600 hover:text-emerald-600 transition-colors"
              >
                <ShoppingCart size={24} />
                {cartItemCount > 0 && (
                  <span className="absolute top-0 right-0 -mt-1 -mr-1 flex items-center justify-center bg-red-500 text-white text-[10px] font-bold h-5 w-5 rounded-full border-2 border-white">
                    {cartItemCount}
                  </span>
                )}
              </button>
              
              {/* Mobile Menu Button */}
              <button 
                className="md:hidden p-2 text-stone-600"
                onClick={() => setIsMobileMenuOpen(!isMobileMenuOpen)}
              >
                {isMobileMenuOpen ? <X size={24} /> : <Menu size={24} />}
              </button>
            </div>
          </div>
        </div>

        {/* Mobile Menu Dropdown */}
        {isMobileMenuOpen && (
          <div className="md:hidden bg-white border-t border-stone-100 px-4 pt-2 pb-6 space-y-1 absolute w-full shadow-lg">
            <button onClick={() => navigate('home')} className="block w-full text-left px-3 py-4 text-stone-800 font-medium border-b border-stone-50">หน้าแรก</button>
            <button onClick={() => navigate('shop')} className="block w-full text-left px-3 py-4 text-stone-800 font-medium border-b border-stone-50">ร้านค้า</button>
            <button onClick={() => navigate('articles')} className="block w-full text-left px-3 py-4 text-stone-800 font-medium border-b border-stone-50">บทความ/วิธีเลี้ยง</button>
            <button onClick={() => navigate('contact')} className="block w-full text-left px-3 py-4 text-stone-800 font-medium">ติดต่อเรา</button>
          </div>
        )}
      </nav>

      {/* Main Content Area (Router Simulation) */}
      <div className="flex-1">
        {currentPage === 'home' && <HomePage />}
        {currentPage === 'shop' && <ShopPage />}
        {currentPage === 'product' && <ProductDetailPage />}
        {currentPage === 'cart' && <CartPage />}
        {currentPage === 'checkoutSuccess' && <CheckoutSuccessPage />}
        {currentPage === 'articles' && <ArticlesPage />}
        {currentPage === 'article' && <ArticleDetailPage />}
        {currentPage === 'contact' && <ContactPage />}
      </div>

      {/* Footer */}
      <footer className="bg-stone-900 text-stone-400 py-12 border-t border-stone-800 mt-auto">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 grid grid-cols-1 md:grid-cols-4 gap-8">
          <div className="col-span-1 md:col-span-2">
            <div className="flex items-center mb-4">
               <div className="w-8 h-8 bg-emerald-600 rounded flex items-center justify-center mr-2">
                <svg className="w-4 h-4 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M13 10V3L4 14h7v7l9-11h-7z" />
                </svg>
              </div>
              <span className="font-extrabold text-xl text-white block">สยามเรปไทล์</span>
            </div>
            <p className="mb-4 max-w-sm">ร้านจำหน่ายงูสวยงามนำเข้า เพาะพันธุ์เอง และอุปกรณ์การเลี้ยงครบวงจร เชื่อมต่อระบบสต๊อกอัจฉริยะ ซื้อขายมั่นใจ</p>
            <p className="text-sm">© {new Date().getFullYear()} Siam Reptiles. All rights reserved.</p>
          </div>
          <div>
            <h4 className="text-white font-bold mb-4">ลิงก์ด่วน</h4>
            <ul className="space-y-2">
              <li><button onClick={() => navigate('home')} className="hover:text-emerald-400 transition-colors">หน้าแรก</button></li>
              <li><button onClick={() => navigate('shop')} className="hover:text-emerald-400 transition-colors">ร้านค้า</button></li>
              <li><button onClick={() => navigate('articles')} className="hover:text-emerald-400 transition-colors">บทความ/วิธีเลี้ยง</button></li>
              <li><button onClick={() => navigate('contact')} className="hover:text-emerald-400 transition-colors">ติดต่อเรา</button></li>
            </ul>
          </div>
          <div>
            <h4 className="text-white font-bold mb-4">ติดต่อเรา</h4>
            <ul className="space-y-2">
              <li>โทร: 080-123-4567</li>
              <li>Line: @siamreptiles</li>
              <li>Facebook: สยามเรปไทล์</li>
              <li>เปิดบริการ: 10:00 - 20:00 น.</li>
            </ul>
          </div>
        </div>
      </footer>

      {/* Global Toast Notification */}
      {toast && (
        <div className="fixed bottom-8 left-1/2 -translate-x-1/2 bg-stone-900 text-white px-6 py-3 rounded-full shadow-2xl flex items-center gap-3 z-[100] transition-all transform duration-300 translate-y-0 opacity-100">
          <CheckCircle2 size={18} className="text-emerald-400" />
          <span className="font-medium text-sm whitespace-nowrap">{toast}</span>
        </div>
      )}
    </div>
  );
}